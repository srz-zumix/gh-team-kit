name: Labeler
on:
  pull_request:
  workflow_dispatch:
    inputs:
      pr-number:
        description: 'The pull request number(s)'
        required: true

jobs:
  labeler:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
      - uses: srz-zumix/labeler-action@bfe41ef86c4392766fd693133fae07de0047cb9d # v0.7.0
        with:
          pr-number: ${{ github.event.inputs.pr-number || github.event.number }}
